Pizza_Store
____________

Admin
______
adminId(Id)
AdminName
UserName
Password 
Email
MobileNo
AdminStatus(YES/NO)


Customer 
_________

CustomerId
FirstName
LastName
Dob
DateOfOpen
Address1
Address2
City
State
ZipCode
Country
Amount
CheqFacil
AccountType
UserName
password
MobileNo
Email
Status 

CREATE DATABASE pizza_store;
USE pizza_store;

-- 1. Customers
CREATE TABLE customers (
    customer_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_name VARCHAR(100),
    phone VARCHAR(15),
    email VARCHAR(100),
    address VARCHAR(255)
);

-- 2. Pizza Master Table
CREATE TABLE pizzas (
    pizza_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    size ENUM('SMALL', 'MEDIUM', 'LARGE'),
    base_price DECIMAL(10,2)
);


-- 5. Orders Table (Order Header)
CREATE TABLE orders (
    order_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_id INT,
    order_date DATETIME DEFAULT CURRENT_TIMESTAMP,
    status ENUM('Pending','Preparing','Out for Delivery','Delivered','Cancelled'),
    total_amount DECIMAL(10,2),
    FOREIGN KEY (customer_id) REFERENCES customers(customer_id)
);

-- 6. Order Items (Pizzas inside an order)
CREATE TABLE order_items (
    order_item_id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT,
    pizza_id INT,
    quantity INT,
    price DECIMAL(10,2),
    FOREIGN KEY (order_id) REFERENCES orders(order_id),
    FOREIGN KEY (pizza_id) REFERENCES pizzas(pizza_id)
);
-- 8. Payments Table
CREATE TABLE payments (
    payment_id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT,
    amount DECIMAL(10,2),
    method ENUM('Cash','Card','UPI'),
    payment_date DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (order_id) REFERENCES orders(order_id)
);

